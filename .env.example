# Public, non-secret browser values may use NEXT_PUBLIC_ prefixes.
NEXT_PUBLIC_SITE_URL=https://linshistudio.com

# Never place production credentials in this file or in NEXT_PUBLIC_* variables.
# Store production-only secrets in the GitHub `github-pages` environment.
# Store test-only secrets in a separate test environment when a future project needs them.
